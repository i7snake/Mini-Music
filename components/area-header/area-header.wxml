<!--components/area-header/area-header.wxml-->
<view class="header">
  <view class="title">{{title}}</view>
  <view class="more" wx:if="{{hasMore}}"  bind:tap="onMore">
    <view class="text">更多</view>
    <van-icon name="arrow" />
  </view>
</view>